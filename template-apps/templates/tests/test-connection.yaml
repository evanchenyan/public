apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "templateApps.fullname" . }}-test-connection"
  labels:
    {{- include "templateApps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "templateApps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
