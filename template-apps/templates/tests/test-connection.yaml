apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "starfire-templates-app.fullname" . }}-test-connection"
  labels:
    {{- include "starfire-templates-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "starfire-templates-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
