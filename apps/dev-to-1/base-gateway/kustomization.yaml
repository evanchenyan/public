apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configurations:
  - kustomizeconfig.yaml
secretGenerator:
  - name: values-base-gateway
    files:
      - values.yaml=values.yaml
resources:
  - release.yaml
